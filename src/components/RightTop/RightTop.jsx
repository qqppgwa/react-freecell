import React from 'react'

// import CardsCol from '../CardsCol/CardsCol.jsx'
const RightTop = ({ cards }) => {
  // console.log(props)

  let col = []
  // for (let i = 0; i < cards.length; i++) {
  //   col.push(<CardsCol card={cards[i]} key={i} />)
  // }
  return <div className="rightTop">{col}</div>
}
export default RightTop
